import { SceneWorker } from './SceneWorker'

// Create instances of non-public-controllers
export async function ensureUiApis(worker: SceneWorker) {
  // TODO: socialController
  // await worker.getAPIInstance(SocialController)
}
